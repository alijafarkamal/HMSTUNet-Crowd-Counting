# Copy to secrets.toml locally (never commit real secrets).
# On Streamlit Community Cloud: App → ⋮ → Settings → Secrets → Save → Reboot app.
#
# Prefer a flat top-level key (case-sensitive). URL must be a DIRECT file download.

CHECKPOINT_URL = "https://huggingface.co/YOUR_USER/YOUR_REPO/resolve/main/best.pth"

# Alternative (nested table — also supported by app.py):
# [checkpoint]
# url = "https://huggingface.co/YOUR_USER/YOUR_REPO/resolve/main/best.pth"
